LINK "../../stdlib/IO.ps"
LINK System
FUNCTION sqrt(INT n)[INT] {
    IF n == 0 {
        RETURN 0
    }
    FLEX guess = n / 2
    FLEX iterations = 10
    
    CYCLE iterations {
        FLEX guess = (guess + n / guess) / 2
    }
    
    RETURN guess
}

FLEX result = sqrt(16)
PRINT(result)