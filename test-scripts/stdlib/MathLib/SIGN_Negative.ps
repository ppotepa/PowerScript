// Test: SIGN - Sign of negative number
// Uses SIGN function from scripts/stdlib/MathLib_Working.ps
// Note: PowerScript doesn't support negative literals or binary expressions in assignments
// So we create a helper function to generate negative values

FUNCTION MAKE_NEG(INT n)[INT] {
    RETURN 0 - n
}

INT value = 42
PRINT SIGN(MAKE_NEG(value))
// Expected output: -1
