// Test: ABS - Absolute value of negative number
// Uses ABS function from scripts/stdlib/MathLib_Working.ps
// Now using nested function calls: ABS(MAKE_NEG(value))

FUNCTION MAKE_NEG(INT n)[INT] {
    RETURN 0 - n
}

INT value = 42
PRINT ABS(MAKE_NEG(value))
// Expected output: 42
