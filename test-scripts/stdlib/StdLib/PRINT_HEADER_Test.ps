LINK "scripts/stdlib/StdLib.ps"

// Test: PRINT_HEADER - Print header with border
STRING title = "Test"
INT width = 20
INT result = PRINT_HEADER(title, width)
PRINT result
// Expected: 0 (and prints boxed header)
