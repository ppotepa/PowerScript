PRINT "Testing scoped variables"

VAR globalX = 100
PRINT "Declared global X"

FUNCTION testScope(INT param) {
    VAR localY = 200
    PRINT "Declared local Y in function"
    RETURN param
}

VAR anotherGlobal = 300
PRINT "All scope tests complete!"
